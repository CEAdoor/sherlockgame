extends layout

block content
    #level
        .container
            .row
                .offset-md-2.col-md-8.text-center
                    .row
                        .col.head_1
                            h1 CASE 2
                    .row.head_2
                        .col
                            img.img-fluid(src="/images/p1.jpg")
                        .col
                            img.img-fluid(src="/images/p2.jpg")
                    .row
                        form(action="/play" method="POST")
                            .form-group
                                input.form-control(type="text" placeholder="Answer" name="option")
                            .form-group
                                input.btn.btn-default.pl-button(type="submit" value="SUBMIT")
    //form.form(action="/play" method="POST")
    //    p this is a question
    //    label(for="option") Options
    //    input(type="text" name="option")
    //    input(type="submit" value="submit")